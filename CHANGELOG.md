# 更新日志

这里记录 CheatSheet 每个版本改了什么。

发布时 GitHub Actions 会自动把**对应版本**的段落抓出来，作为 Release 的说明
（见 `.github/workflows/release.yml`）。所以发新版本前，记得在这里先写好一段。

格式沿用 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号沿用 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 新增

- **图片 / Markdown 笔记 / PDF 也能丢进小窗**:图片直接看(多张可上下切换),
  `.md` / `.txt` 用轻量 Markdown 渲染(标题、列表、引用、代码块、加粗、链接),
  **PDF 交给内置浏览器**(Edge 自带的 PDF 查看器,能滚动能缩放,不用额外依赖)。
  拖放和"打开"对话框都按类型自动分流
- **「最近观看」下拉栏**(控制条):列出最近看过的文件 + 上次的进度,点一下接着看;
  文件已经不在硬盘上的会自动清掉
- **位置预设**:设置里一键把窗口贴到屏幕某个角(按显示器工作区算,避开任务栏)
- **鼠标穿透**(`Ctrl+Alt+P`):点击直接穿过小窗落到游戏上,小窗只当"看板";
  开着时控制条上会挂一个「穿透中」标记
- **补齐全局热键**:上一集 / 下一集、倍速切换、音量 ±、鼠标穿透。
  设置里照样能改键,而且按键名现在会显示成「↑ / , / 1」这种看得懂的写法
- 网页模式**播完自动接下一P**(最后一P 且开着循环则回到第一P),
  切集后还会轻推一把确保新的一P 真的开始播
- 网页模式下控制条多了**「播放器」下拉栏**:把播放器自己的**画质**和**弹幕开关**搬了出来,
  切画质不用再去点小窗里那套面板(需要登录 / 大会员的档位由网站自己判断)

## [v0.8] - 2026-09-14

### 新增

- **检查更新**(设置 → 关于):查 GitHub Releases 上有没有新版本,有就给出下载页;
  默认启动时自动查一次(**一天最多一次**),只在发现新版时提示一句。
  那个页面里还能一键**打开配置目录**(`settings.json` 和崩溃日志都在那儿)。
  「查不到」和「已经是最新」会分开提示,不会让网络问题看起来像是最新版
- **自动更新**:发现新版本后可以直接「下载并更新」—— 下载 → 解压到临时目录 →
  **等程序退出后替换程序目录并自动重启**(运行中的程序覆盖不了自己,所以由一个小脚本接手)。
  整个过程中原程序一个字节都不动,替换失败会保留原样并在下次启动时提示;
  不喜欢自动替换的,「打开下载页」随时可用

### 修复

- **透明度快捷键改不动「设置的透明度」**(v0.7 的回归):上一版把设置窗口的滑块改成读
  **用户设定值**之后,快捷键那条路径的破绽就露出来了 —— 它只改画面、从不写设置里的值,
  所以滑块不会跟着动(以前滑块读的是画面当前值,看着像跟随,其实设置值一直是旧的、关掉还会丢)。
  现在快捷键和滑块走同一条路径:画面 / 设置值 / 滑块三者一致,改动也会随设置保存
- **合集视频的「当前集」可能指错**:分P 段和合集段都带同一个高亮类时,
  原来取最后一个会指到别的段上 —— 现在取第一个(选集下拉栏的高亮项与当前集名)

## [v0.7] - 2026-09-14

### 新增

- 控制条上多了**选集下拉栏**:网页模式列出 B 站**多P 视频的分P**,点一下切集 ——
  走**播放器自己的选集逻辑**(站内切换,页面不重新加载),网页全屏和进度都不被打断;
  在页面里自己切集也会同步,控制条的「上一集 / 下一集」在网页模式下同样是切分P。
  本地模式下列出**播放列表**,选中即切文件
- **本地视频记住播放进度**:看到一半关掉,下次打开同一个文件**接着上次的位置继续**;
  看完的那一集不会留记录,设置里也能整个关掉
- **循环播放**开关(设置 → 播放):播完最后一集绕回第一集,只有一集就是重播自己。
  这个配置项以前就存在、但没有任何界面入口,那段循环逻辑是一直走不到的死代码
- **单实例**:重复启动不会起第二个 —— 第二个实例把这次要打开的文件通过命名管道
  交给已经在跑的那个,然后自己退出(以前会多出一条控制条、热键还注册不上);
  没带文件启动时,第一个实例会提示一句"已经在运行了"

### 变更

- **设置窗口重排成三个标签页**(播放 / 显示与声音 / 快捷键),以前所有设置挤在一长条里;
  「恢复默认快捷键」挪进快捷键页,窗口也窄了一点

### 修复

- **网页模式下控制条上那行字不会变**:一直挂着本地视频的占位提示
  ("把视频拖到这里开始播放"),看着像坏了。现在网页模式显示**网页标题**,
  有多P 时显示**当前那一集的名字**(B 站标题末尾的「_哔哩哔哩_bilibili」会砍掉),
  窗口标题也跟着走
- **网页模式下的倍速显示是假的**:显示的是本地那份倍速 —— 在 B 站自己的控制条上改成 1.5x,
  app 里还写着 1x,切换倍速也从错的档位往上走。现在以页面里那个 `<video>` 的实际倍速为准
- **暂停压暗会在「30% / 原透明度」之间闪**:一是暂停中动一下透明度会把"正在压暗"的状态清掉、
  下一轮轮询又压回去;二是设置窗口的滑块读的是当前视觉值(压暗时正好 30),一碰就从 30 起跳。
  现在两者都以**用户设定的值**为准,压暗期间画面稳定停在 30%
- **崩溃兜底**:UI 线程的异常现在会接住 —— 弹一个窗说清**哪里出错**(异常类型、消息、
  调用栈前几帧),完整信息写到 `%AppData%\CheatSheet\crash-*.log`,然后干净退出。
  以前是直接崩掉:系统弹 WER 框,而且播放窗口会半死不活地留在屏幕最上层

## [v0.6] - 2026-09-13

### 新增

- **网页模式:直接把小窗当浏览器用**(默认首页 B 站)。不用先下视频,边玩游戏边看网页攻略;
  由设置里的「浏览器 / 本地视频」切换
- 控制条按钮行上面多了一行**地址栏**(只在网页模式出现),可以随手输网址;
  粘贴带链接的**分享口令**(如 B 站的「复制此链接」)会自动抠出里头的链接,前后文字不会被当成 URL
- 网页模式下也有**进度条**了(以前网页模式下是收起来的):位置和总时长是从网页里那个视频读来的,
  点一下跳转、直接拖都行,和本地视频模式手感一致
- 网页模式下**全局热键、倍速、快进/长按 2 倍速、暂停时的变暗/隐藏全都作用在网页里那个视频上**
- 项目 **logo**(`docs/logo.svg`):一张写着答案、按下播放键的小纸条 —— 正是 CheatSheet(小抄)的寓意

### 变更

- **去掉「视频 / 网页模式切换」的全局热键**(原来绑在 `Ctrl+Alt+B`)。切模式现在只走设置面板,
  免得在游戏里误按一下,整个播放器就换成浏览器了 —— 而且换回来还要重新加载页面。
- **退出网页模式会把内置浏览器整个关掉**(以前只是折叠起来藏着)。WebView2 背后是一组 Edge 进程,
  动辄几百 MB,还带着后台 GPU / 网络活动 —— 现在切回本地视频模式的瞬间就把内存还给系统。
  再切回网页模式会重新加载页面:地址接着上次那个,播放位置不保留。

### 修复

- **网页全屏后上方留着一条 B 站白条**:那条白条是 B 站自己的固定导航栏。
  以前是自己写 CSS 把 `<video>` 拉成 `position:fixed`,只动了视频元素、没接管网站的布局层级;
  现在改成**点一下网站自己的「网页全屏」按钮**(B 站那个功能是纯 CSS 实现的),
  网站会把整个播放器连同控制条、弹幕层一起抬到最上层,不留残渣
- **网页模式下鼠标操作不了页面**:WebView2 Composition 控件自带的那套鼠标转发依赖"命中测试选中它",
  在分层透明窗口里不生效,鼠标全被画面接走了。现在在窗口级 `Preview` 事件里**自己把鼠标转发给浏览器**
- **网页模式下改不了播放进度/切不了倍速**:这几个动作以前只看本地 `MediaElement` 的状态,
  网页模式下永远是"没在播放"。现在播放状态改成两种模式各算各的
- 网页模式下的**拖窗口**改成只认画面顶部那一条,画面其余部分整个让给网页

## [v0.5] - 2026-09-12

### 修复

- **没打开视频时不再误触发「暂停时」行为**：以前程序一启动就被当成"暂停中" ——
  选「调暗到 30%」会发现提示文字是暗的，选「隐藏视频窗口」则**窗口一启动就不见**。
  现在只有真正打开过视频，才会按设置切换暂停 / 播放状态。

## [v0.4] - 2026-09-12

### 新增

- 设置里可选**暂停时的处理方式**：什么都不做 / 隐藏视频窗口 / 把画面调暗到 30%（默认，推荐）
- 控制条**出现和收起时加了动画**（淡入淡出 + 轻微滑动，不再生硬地闪出来）
- 新增本更新日志，并接进自动发布流程

### 修复

- **按 `Ctrl+Alt+T` 显示/隐藏窗口不再打断游戏**：改用 `SW_HIDE` / `SW_SHOWNOACTIVATE`，
  显示和隐藏都不再抢前台。以前窗口一出来就抢走焦点，游戏收到 `WM_KILLFOCUS` 会清掉按键状态，
  表现就是"按住 `A` 走路时切一下窗口，人就走不动了，得重按一次"

## [v0.3] - 2026-09-12

### 修复

- **按热键后不用再点一下游戏**：所有热键统一走 `RunHotkey`，执行完把前台还给原来的窗口
- **空格不再能控制播放**：删掉了内置的那套窗口快捷键（`Space` / `←→` / `↑↓` / `M`），
  现在只认你自己配的全局热键
- **长按快进不再"粘住"**：修掉收尾逻辑里的提前返回 —— 以前会导致倍速卡在 2 倍、
  之后的快进按键被直接吞掉

## [v0.2] - 2026-09-12

### 新增

- 设置里可选**等比例拖动**（拖窗口边缘时按视频比例缩放）

### 变更

- 窗口改为**常置顶**（不再有置顶开关）
- `Ctrl+Alt+T` 改为**显示 / 隐藏播放窗口**

### 修复

- 修掉右键弹出的 **"Clear"** 菜单（来自 HandyControl Growl 宿主的上下文菜单）
- 播放窗口与输入法解绑，避免输入法浮层挂到画面上
- 进度圆点位置与进度精确对应；拖动判定从 1px 放大到 16px
- 进度条贴到窗口底边、左右不留空隙
- 打开设置窗口不再覆盖已设的透明度 / 音量

## [v0.1] - 2026-09-12

首个版本。

### 新增

- 置顶小窗播放本地视频（`.mp4` / `.m4v` / `.mov` / `.wmv` / `.avi` / `.mkv` / `.webm` / `.flv` / `.ts` / `.mpg` / `.mpeg` / `.3gp`）
- 屏幕正上方的**独立控制条**，鼠标移到屏幕顶部才出现，点击不抢游戏焦点
- 画面透明度、音量、倍速、静音、上一集 / 下一集
- 全局热键（Win32 `RegisterHotKey`），支持单键
- 支持把视频文件拖进窗口播放，也支持命令行传入文件路径

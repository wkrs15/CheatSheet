# 更新日志

这里记录 CheatSheet 每个版本改了什么。

发布时 GitHub Actions 会自动把**对应版本**的段落抓出来，作为 Release 的说明
（见 `.github/workflows/release.yml`）。所以发新版本前，记得在这里先写好一段。

格式沿用 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号沿用 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 新增

- **网页模式:直接把小窗当浏览器用**(默认首页 B 站)。不用先下视频,边玩游戏边看网页攻略;
  由设置里的「浏览器 / 本地视频」切换
- 控制条按钮行上面多了一行**地址栏**(只在网页模式出现),可以随手输网址;
  粘贴带链接的**分享口令**(如 B 站的「复制此链接」)会自动抠出里头的链接,前后文字不会被当成 URL
- 网页模式下也有**进度条**了(以前网页模式下是收起来的):位置和总时长是从网页里那个视频读来的,
  点一下跳转、直接拖都行,和本地视频模式手感一致
- 网页模式下**全局热键、倍速、快进/长按 2 倍速、暂停时的变暗/隐藏全都作用在网页里那个视频上**

### 变更

- **去掉「视频 / 网页模式切换」的全局热键**(原来绑在 `Ctrl+Alt+B`)。切模式现在只走设置面板,
  免得在游戏里误按一下,整个播放器就换成浏览器了 —— 而且换回来还要重新加载页面。
- **退出网页模式会把内置浏览器整个关掉**(以前只是折叠起来藏着)。WebView2 背后是一组 Edge 进程,
  动辄几百 MB,还带着后台 GPU / 网络活动 —— 现在切回本地视频模式的瞬间就把内存还给系统。
  再切回网页模式会重新加载页面:地址接着上次那个,播放位置不保留。

### 修复

- **网页全屏后上方留着一条 B 站白条**:那条白条是 B 站自己的固定导航栏。
  以前是自己写 CSS 把 `<video>` 拉成 `position:fixed`,只动了视频元素、没接管网站的布局层级;
  现在改成**点一下网站自己的「网页全屏」按钮**(B 站那个功能是纯 CSS 实现的),
  网站会把整个播放器连同控制条、弹幕层一起抬到最上层,不留残渣
- **网页模式下鼠标操作不了页面**:WebView2 Composition 控件自带的那套鼠标转发依赖"命中测试选中它",
  在分层透明窗口里不生效,鼠标全被画面接走了。现在在窗口级 `Preview` 事件里**自己把鼠标转发给浏览器**
- **网页模式下改不了播放进度/切不了倍速**:这几个动作以前只看本地 `MediaElement` 的状态,
  网页模式下永远是"没在播放"。现在播放状态改成两种模式各算各的
- 网页模式下的**拖窗口**改成只认画面顶部那一条,画面其余部分整个让给网页

## [v0.5] - 2026-09-12

### 修复

- **没打开视频时不再误触发「暂停时」行为**：以前程序一启动就被当成"暂停中" ——
  选「调暗到 30%」会发现提示文字是暗的，选「隐藏视频窗口」则**窗口一启动就不见**。
  现在只有真正打开过视频，才会按设置切换暂停 / 播放状态。

## [v0.4] - 2026-09-12

### 新增

- 设置里可选**暂停时的处理方式**：什么都不做 / 隐藏视频窗口 / 把画面调暗到 30%（默认，推荐）
- 控制条**出现和收起时加了动画**（淡入淡出 + 轻微滑动，不再生硬地闪出来）
- 新增本更新日志，并接进自动发布流程

### 修复

- **按 `Ctrl+Alt+T` 显示/隐藏窗口不再打断游戏**：改用 `SW_HIDE` / `SW_SHOWNOACTIVATE`，
  显示和隐藏都不再抢前台。以前窗口一出来就抢走焦点，游戏收到 `WM_KILLFOCUS` 会清掉按键状态，
  表现就是"按住 `A` 走路时切一下窗口，人就走不动了，得重按一次"

## [v0.3] - 2026-09-12

### 修复

- **按热键后不用再点一下游戏**：所有热键统一走 `RunHotkey`，执行完把前台还给原来的窗口
- **空格不再能控制播放**：删掉了内置的那套窗口快捷键（`Space` / `←→` / `↑↓` / `M`），
  现在只认你自己配的全局热键
- **长按快进不再"粘住"**：修掉收尾逻辑里的提前返回 —— 以前会导致倍速卡在 2 倍、
  之后的快进按键被直接吞掉

## [v0.2] - 2026-09-12

### 新增

- 设置里可选**等比例拖动**（拖窗口边缘时按视频比例缩放）

### 变更

- 窗口改为**常置顶**（不再有置顶开关）
- `Ctrl+Alt+T` 改为**显示 / 隐藏播放窗口**

### 修复

- 修掉右键弹出的 **"Clear"** 菜单（来自 HandyControl Growl 宿主的上下文菜单）
- 播放窗口与输入法解绑，避免输入法浮层挂到画面上
- 进度圆点位置与进度精确对应；拖动判定从 1px 放大到 16px
- 进度条贴到窗口底边、左右不留空隙
- 打开设置窗口不再覆盖已设的透明度 / 音量

## [v0.1] - 2026-09-12

首个版本。

### 新增

- 置顶小窗播放本地视频（`.mp4` / `.m4v` / `.mov` / `.wmv` / `.avi` / `.mkv` / `.webm` / `.flv` / `.ts` / `.mpg` / `.mpeg` / `.3gp`）
- 屏幕正上方的**独立控制条**，鼠标移到屏幕顶部才出现，点击不抢游戏焦点
- 画面透明度、音量、倍速、静音、上一集 / 下一集
- 全局热键（Win32 `RegisterHotKey`），支持单键
- 支持把视频文件拖进窗口播放，也支持命令行传入文件路径
